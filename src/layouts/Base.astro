---
import {
	getMenu,
	getEmDashCollection,
	getSiteSettings,
	getTaxonomyTerms,
} from "emdash";
import {
	WidgetArea,
	EmDashHead,
	EmDashBodyStart,
	EmDashBodyEnd,
} from "emdash/ui";
import { createPublicPageContext } from "emdash/page";
import LiveSearch from "emdash/ui/search";
import "../styles/theme.css";

interface Props {
	title: string;
	description?: string | null;
	image?: string | null;
	canonical?: string | null;
	robots?: string | null;
	type?: "website" | "article";
	publishedTime?: string | null;
	modifiedTime?: string | null;
	author?: string | null;
	content?: { collection: string; id: string; slug?: string | null };
}

const {
	title,
	description,
	image,
	canonical,
	robots,
	type = "website",
	publishedTime,
	modifiedTime,
	author,
	content,
} = Astro.props;

const siteSettings = await getSiteSettings();
const siteTitle = siteSettings.title?.trim() || "The Sonderise Chronicle";
const siteTagline =
	siteSettings.tagline?.trim() ||
	"Independent dispatches on design, systems, and digital culture.";
const fullTitle = title.includes(siteTitle) ? title : `${title} | ${siteTitle}`;

const menu = await getMenu("primary");
const menuItems = menu?.items ?? [];
const categories = await getTaxonomyTerms("category");
const sectionLinks = categories.slice(0, 5);
const { entries: pages } = await getEmDashCollection("pages", { limit: 6 });
const footerPages = pages.slice(0, 4);

const pageCtx = createPublicPageContext({
	Astro,
	kind: content ? "content" : "custom",
	pageType: type,
	title: fullTitle,
	description,
	canonical,
	image,
	content,
	seo: { ogImage: image, robots },
	articleMeta: { publishedTime, modifiedTime, author },
	siteName: siteTitle,
});

const isLoggedIn = !!Astro.locals.user;
const editionDateShort = new Intl.DateTimeFormat("en-US", {
	month: "short",
	day: "numeric",
}).format(new Date());
const editionIso = new Date().toISOString();
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Newsreader:opsz,wght@6..72,400;6..72,500;6..72,600;6..72,700&display=swap"
			rel="stylesheet"
		/>
		<title>{fullTitle}</title>
		<EmDashHead page={pageCtx} />
		<script is:inline>
			(function () {
				var c = document.cookie;
				var i = c.indexOf("theme=");
				var theme = i >= 0 ? c.slice(i + 6).split(";")[0] : null;
				if (theme === "dark" || theme === "light") {
					document.documentElement.classList.add(theme);
				} else if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
					document.documentElement.classList.add("dark");
				}
			})();
		</script>
	</head>
	<body>
		<EmDashBodyStart page={pageCtx} />
		<header class="site-header">
			<div class="site-header__inner">
				<div class="masthead__brand">
					<a href="/" class="site-title">{siteTitle}</a>
					<p class="site-title-meta">
						<span class="edition-kicker">Independent Journal</span>
						<time datetime={editionIso}>{editionDateShort}</time>
					</p>
				</div>

				<nav class="site-nav" aria-label="Primary">
					<ul class="nav-links">
						{menuItems.length > 0 ? (
							menuItems.map((item) => (
								<li
									class:list={[
										"nav-item",
										item.children.length > 0 && "nav-item--has-children",
									]}
								>
									<a
										href={item.url}
										target={item.target}
										rel={
											item.target === "_blank" ? "noopener noreferrer" : undefined
										}
									>
										{item.label}
									</a>
									{
										item.children.length > 0 && (
											<ul class="nav-submenu">
												{item.children.map((child) => (
													<li>
														<a
															href={child.url}
															target={child.target}
															rel={
																child.target === "_blank"
																	? "noopener noreferrer"
																	: undefined
															}
														>
															{child.label}
														</a>
													</li>
												))}
											</ul>
										)
									}
								</li>
							))
						) : (
							<li class="nav-item">
								<a href="/posts">Latest Stories</a>
							</li>
						)}
					</ul>

				</nav>

				<div class="masthead__tools">
					<LiveSearch
						placeholder="Search"
						class="site-search"
						inputClass="site-search-input"
						resultsClass="site-search-results"
						resultClass="site-search-result"
						collections={["posts", "pages"]}
						expandOnFocus={{ collapsed: "10rem", expanded: "14rem" }}
					/>
					{
						isLoggedIn && (
							<a href="/_emdash/admin" class="nav-admin">
								Newsroom
							</a>
						)
					}
				</div>
			</div>
		</header>

		<main>
			<slot />
		</main>

		<footer class="site-footer">
			<div class="footer-inner">
				<div class="footer-grid">
					<div class="footer-brand">
						<p class="footer-kicker">From the News Desk</p>
						<a href="/" class="footer-logo">{siteTitle}</a>
						<p class="footer-tagline">{siteTagline}</p>
					</div>

					<div class="footer-nav">
						<h4 class="footer-heading">Read</h4>
						<ul class="footer-links">
							<li><a href="/">Front Page</a></li>
							<li><a href="/posts">Latest Stories</a></li>
							<li><a href="/search">Search</a></li>
							<li><a href="/rss.xml">RSS Feed</a></li>
						</ul>
					</div>

					{
						sectionLinks.length > 0 && (
							<div class="footer-nav">
								<h4 class="footer-heading">Sections</h4>
								<ul class="footer-links">
									{sectionLinks.map((category) => (
										<li>
											<a href={`/category/${category.slug}`}>{category.label}</a>
										</li>
									))}
								</ul>
							</div>
						)
					}

					<div class="footer-nav">
						<h4 class="footer-heading">Pages</h4>
						<ul class="footer-links">
							{footerPages.map((page) => (
								<li>
									<a href={`/pages/${page.data.slug || page.id}`}>
										{page.data.title}
									</a>
								</li>
							))}
						</ul>
					</div>

					<div class="footer-widgets-section">
						<WidgetArea name="footer" />
					</div>
				</div>

				<div class="footer-bottom">
					<p class="footer-copyright">
						Built on <a href="https://emdashcms.com">EmDash</a> for thoughtful
						digital publishing.
					</p>

					<div class="footer-controls">
						<div class="theme-switcher">
							<button
								type="button"
								class="theme-btn"
								data-theme="light"
								aria-label="Light mode"
							>
								<svg
									width="16"
									height="16"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<circle cx="12" cy="12" r="5"></circle>
									<line x1="12" y1="1" x2="12" y2="3"></line>
									<line x1="12" y1="21" x2="12" y2="23"></line>
									<line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
									<line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
									<line x1="1" y1="12" x2="3" y2="12"></line>
									<line x1="21" y1="12" x2="23" y2="12"></line>
									<line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
									<line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
								</svg>
							</button>
							<button
								type="button"
								class="theme-btn"
								data-theme="dark"
								aria-label="Dark mode"
							>
								<svg
									width="16"
									height="16"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
								</svg>
							</button>
							<button
								type="button"
								class="theme-btn"
								data-theme="system"
								aria-label="System theme"
							>
								<svg
									width="16"
									height="16"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
									<line x1="8" y1="21" x2="16" y2="21"></line>
									<line x1="12" y1="17" x2="12" y2="21"></line>
								</svg>
							</button>
						</div>
					</div>
				</div>
			</div>
		</footer>

		<script>
			const THEME_REGEX = /theme=([^;]+)/;
			const themeBtns =
				document.querySelectorAll<HTMLButtonElement>(".theme-btn");
			const root = document.documentElement;

			function setCookie(
				name: string,
				value: string,
				maxAge: number = 31536000
			) {
				const secure = location.protocol === "https:" ? "; Secure" : "";
				if (value === "") {
					document.cookie = `${name}=; path=/; max-age=0; SameSite=Lax${secure}`;
				} else {
					document.cookie = `${name}=${value}; path=/; max-age=${maxAge}; SameSite=Lax${secure}`;
				}
			}

			function setTheme(theme: string) {
				if (theme === "system") {
					setCookie("theme", "");
					root.classList.remove("light", "dark");
					if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
						root.classList.add("dark");
					}
				} else {
					setCookie("theme", theme);
					root.classList.remove("light", "dark");
					root.classList.add(theme);
				}
				updateActiveBtn(theme);
			}

			function updateActiveBtn(theme: string) {
				themeBtns.forEach((btn) => {
					btn.classList.toggle("active", btn.dataset.theme === theme);
				});
			}

			function getStoredTheme(): string {
				const match = document.cookie.match(THEME_REGEX);
				return match ? match[1] : "system";
			}

			const storedTheme = getStoredTheme();
			setTheme(storedTheme);

			themeBtns.forEach((btn) => {
				btn.addEventListener("click", () => {
					setTheme(btn.dataset.theme || "system");
				});
			});

			window
				.matchMedia("(prefers-color-scheme: dark)")
				.addEventListener("change", (e) => {
					if (getStoredTheme() === "system") {
						root.classList.toggle("dark", e.matches);
					}
				});
		</script>

		<style is:global>
			*:where(:not([class*="emdash"]):not([class*="ec-"])),
			*:where(:not([class*="emdash"]):not([class*="ec-"]))::before,
			*:where(:not([class*="emdash"]):not([class*="ec-"]))::after {
				box-sizing: border-box;
			}

			body,
			h1,
			h2,
			h3,
			h4,
			h5,
			h6,
			p,
			ul,
			ol,
			figure,
			blockquote,
			dl,
			dd {
				margin: 0;
			}

			ul,
			ol {
				padding: 0;
			}

			:root {
				--color-bg: var(--theme-color-bg, #f5f0e6);
				--color-bg-subtle: var(--theme-color-bg-subtle, #ece4d2);
				--color-text: var(--theme-color-text, #17120d);
				--color-text-secondary: var(--theme-color-text-secondary, #4e463d);
				--color-muted: var(--theme-color-muted, #6d655b);
				--color-border: var(--theme-color-border, #ccbca0);
				--color-border-subtle: var(--theme-color-border-subtle, #ded2bb);
				--color-surface: var(--theme-color-surface, #fcf8f1);
				--color-accent: var(--theme-color-accent, #8b2a1b);
				--color-accent-hover: var(--theme-color-accent-hover, #672015);
				--color-on-accent: var(--theme-color-on-accent, #fffaf2);
				--color-accent-ring: var(
					--theme-color-accent-ring,
					color-mix(in srgb, var(--color-accent) 25%, transparent)
				);

				--emdash-search-bg: var(--color-bg);
				--emdash-search-text: var(--color-text);
				--emdash-search-muted: var(--color-muted);
				--emdash-search-border: var(--color-border);
				--emdash-search-hover: var(--color-surface);
				--emdash-search-highlight: var(--color-text);

				--font-sans:
					var(
						--theme-font-sans,
						"Source Sans 3",
						"Helvetica Neue",
						Arial,
						sans-serif
					);
				--font-serif:
					var(--theme-font-serif, "Source Serif 4", Georgia, serif);
				--font-display:
					var(
						--theme-font-display,
						"Cormorant Garamond",
						"Times New Roman",
						serif
					);
				--font-mono:
					var(
						--theme-font-mono,
						"IBM Plex Mono",
						ui-monospace,
						SFMono-Regular,
						Menlo,
						monospace
					);

				--font-size-xs: var(--theme-font-size-xs, 0.8rem);
				--font-size-sm: var(--theme-font-size-sm, 0.9375rem);
				--font-size-base: var(--theme-font-size-base, 1.0625rem);
				--font-size-lg: var(--theme-font-size-lg, 1.2rem);
				--font-size-xl: var(--theme-font-size-xl, 1.5rem);
				--font-size-2xl: var(--theme-font-size-2xl, 2rem);
				--font-size-3xl: var(--theme-font-size-3xl, 2.6rem);
				--font-size-4xl: var(--theme-font-size-4xl, 3.4rem);
				--font-size-5xl: var(--theme-font-size-5xl, 4.75rem);

				--leading-tight: var(--theme-leading-tight, 0.98);
				--leading-snug: var(--theme-leading-snug, 1.18);
				--leading-normal: var(--theme-leading-normal, 1.5);
				--leading-relaxed: var(--theme-leading-relaxed, 1.72);

				--spacing-1: 0.25rem;
				--spacing-2: 0.5rem;
				--spacing-3: 0.75rem;
				--spacing-4: 1rem;
				--spacing-5: 1.25rem;
				--spacing-6: 1.5rem;
				--spacing-8: 2rem;
				--spacing-10: 2.5rem;
				--spacing-12: 3rem;
				--spacing-16: 4rem;
				--spacing-20: 5rem;
				--spacing-24: 6rem;

				--content-width: var(--theme-content-width, 740px);
				--wide-width: var(--theme-wide-width, 1280px);
				--max-width: var(--content-width);
				--gutter-width: var(--theme-gutter-width, 250px);
				--meta-col-width: var(--theme-meta-col-width, 200px);
				--nav-height: var(--theme-nav-height, 72px);
				--search-input-width: var(--theme-search-input-width, 180px);

				--radius: var(--theme-radius, 0px);
				--radius-lg: var(--theme-radius-lg, 14px);
				--rule-thick: var(--theme-rule-thick, 3px);

				--transition-fast: 140ms ease;
				--transition-base: 220ms ease;

				--avatar-size-xs: 18px;
				--avatar-size-sm: 20px;
				--avatar-size-md: 24px;
				--avatar-size-lg: 32px;

				--tracking-tight: var(--theme-tracking-tight, -0.035em);
				--tracking-snug: var(--theme-tracking-snug, -0.025em);
				--tracking-wide: var(--theme-tracking-wide, 0.08em);
				--tracking-wider: var(--theme-tracking-wider, 0.14em);

				--tag-padding-y: 0.25rem;

				--shadow-dropdown: var(
					--theme-shadow-dropdown,
					0 20px 48px rgba(33, 22, 12, 0.16)
				);
				--shadow-btn-active: var(
					--theme-shadow-btn-active,
					0 1px 3px rgba(16, 10, 4, 0.12)
				);
			}

			@media (prefers-color-scheme: dark) {
				:root:not(.light) {
					--color-bg: var(--theme-dark-color-bg, #14100c);
					--color-bg-subtle: var(--theme-dark-color-bg-subtle, #1b1611);
					--color-text: var(--theme-dark-color-text, #f5efe6);
					--color-text-secondary: var(--theme-dark-color-text-secondary, #d8ccb9);
					--color-muted: var(--theme-dark-color-muted, #9b8d79);
					--color-border: var(--theme-dark-color-border, #4b3b29);
					--color-border-subtle: var(--theme-dark-color-border-subtle, #35291d);
					--color-surface: var(--theme-dark-color-surface, #211a14);
					--color-accent: var(--theme-dark-color-accent, #d46c58);
					--color-accent-hover: var(--theme-dark-color-accent-hover, #eb8b79);
					--color-on-accent: var(--theme-dark-color-on-accent, #1a120d);
					--color-accent-ring: var(
						--theme-dark-color-accent-ring,
						color-mix(in srgb, var(--color-accent) 30%, transparent)
					);
				}
			}

			:root.dark {
				--color-bg: var(--theme-dark-color-bg, #14100c);
				--color-bg-subtle: var(--theme-dark-color-bg-subtle, #1b1611);
				--color-text: var(--theme-dark-color-text, #f5efe6);
				--color-text-secondary: var(--theme-dark-color-text-secondary, #d8ccb9);
				--color-muted: var(--theme-dark-color-muted, #9b8d79);
				--color-border: var(--theme-dark-color-border, #4b3b29);
				--color-border-subtle: var(--theme-dark-color-border-subtle, #35291d);
				--color-surface: var(--theme-dark-color-surface, #211a14);
				--color-accent: var(--theme-dark-color-accent, #d46c58);
				--color-accent-hover: var(--theme-dark-color-accent-hover, #eb8b79);
				--color-on-accent: var(--theme-dark-color-on-accent, #1a120d);
				--color-accent-ring: var(
					--theme-dark-color-accent-ring,
					color-mix(in srgb, var(--color-accent) 30%, transparent)
				);
			}

			html {
				scroll-behavior: smooth;
			}

			body {
				font-family: var(--font-serif);
				font-size: var(--font-size-base);
				line-height: var(--leading-relaxed);
				color: var(--color-text);
				background:
					radial-gradient(
						circle at 0% 0%,
						color-mix(in srgb, var(--color-accent) 14%, transparent),
						transparent 34%
					),
					radial-gradient(
						circle at 84% 14%,
						color-mix(in srgb, white 48%, var(--color-accent) 10%),
						transparent 26%
					),
					linear-gradient(
						180deg,
						color-mix(in srgb, white 44%, var(--color-bg)) 0%,
						var(--color-bg) 34%,
						color-mix(in srgb, var(--color-bg-subtle) 56%, var(--color-bg)) 100%
					);
				background-attachment: fixed;
				-webkit-font-smoothing: antialiased;
				-moz-osx-font-smoothing: grayscale;
				min-height: 100vh;
				position: relative;
				text-wrap: pretty;
			}

			body::before {
				content: "";
				position: fixed;
				inset: 0;
				background:
					linear-gradient(
						to right,
						color-mix(in srgb, var(--color-border-subtle) 58%, transparent) 1px,
						transparent 1px
					),
					linear-gradient(
						to bottom,
						color-mix(in srgb, var(--color-border-subtle) 50%, transparent) 1px,
						transparent 1px
					);
				background-size: 72px 72px;
				opacity: 0.2;
				pointer-events: none;
				z-index: 0;
			}

			a:where(:not([class*="emdash"]):not([class*="ec-"])) {
				color: var(--color-accent);
				text-decoration: none;
				transition:
					color var(--transition-fast),
					opacity var(--transition-fast);
			}

			a:where(:not([class*="emdash"]):not([class*="ec-"])):hover {
				color: var(--color-accent-hover);
			}

			img {
				max-width: 100%;
				height: auto;
				display: block;
			}

			button,
			input,
			textarea,
			select {
				font: inherit;
				font-family: var(--font-sans);
			}

			h1,
			h2,
			h3,
			h4,
			h5,
			h6 {
				font-family: var(--font-display);
				line-height: var(--leading-tight);
				font-weight: 600;
				letter-spacing: var(--tracking-snug);
			}

			h1 {
				font-weight: 700;
				letter-spacing: var(--tracking-tight);
			}

			::selection {
				background: var(--color-accent);
				color: var(--color-on-accent);
			}
		</style>

		<style>
			.site-header,
			main,
			.site-footer {
				position: relative;
				z-index: 1;
			}

			.site-header {
				position: sticky;
				top: 0;
				z-index: 100;
				background: color-mix(in srgb, var(--color-bg) 86%, transparent);
				backdrop-filter: blur(16px) saturate(135%);
				-webkit-backdrop-filter: blur(16px) saturate(135%);
				border-bottom: 1px solid color-mix(in srgb, var(--color-border) 88%, transparent);
			}

			.site-header__inner,
			.footer-inner {
				max-width: var(--wide-width);
				margin: 0 auto;
				padding-left: var(--spacing-6);
				padding-right: var(--spacing-6);
			}

			.site-header__inner {
				display: grid;
				grid-template-columns: auto minmax(0, 1fr) auto;
				align-items: center;
				gap: 1rem;
				min-height: var(--nav-height);
				padding-top: 0.65rem;
				padding-bottom: 0.65rem;
			}

			.masthead__brand {
				display: flex;
				flex-direction: column;
				gap: 0.32rem;
				max-width: 22rem;
			}

			.site-title {
				font-family: var(--font-sans);
				font-size: clamp(1.15rem, 1.4vw, 1.45rem);
				font-weight: 800;
				line-height: 0.98;
				letter-spacing: -0.035em;
				color: var(--color-text);
				text-decoration: none;
			}

			.site-title:hover {
				color: var(--color-text);
			}

			.site-title-meta {
				display: flex;
				align-items: center;
				flex-wrap: wrap;
				gap: 0.55rem;
				font-family: var(--font-sans);
				font-size: 0.72rem;
				font-weight: 700;
				letter-spacing: 0.12em;
				text-transform: uppercase;
				color: var(--color-muted);
			}

			.edition-kicker {
				display: inline-flex;
				align-items: center;
				gap: 0.45rem;
				font-weight: 800;
				color: var(--color-text-secondary);
			}

			.edition-kicker::before {
				content: "";
				width: 0.38rem;
				height: 0.38rem;
				border-radius: 999px;
				background: var(--color-accent);
			}

			.masthead__tools {
				display: flex;
				align-items: center;
				justify-content: flex-end;
				gap: 0.65rem;
			}

			.site-nav {
				min-width: 0;
			}

			.nav-links {
				display: flex;
				align-items: center;
				flex-wrap: wrap;
				gap: 0.9rem;
				list-style: none;
			}

			.nav-item {
				position: relative;
			}

			.nav-links a {
				display: inline-flex;
				align-items: center;
				padding: 0.3rem 0.15rem;
				font-size: 0.8rem;
				font-weight: 700;
				letter-spacing: 0.08em;
				text-transform: uppercase;
				color: var(--color-text-secondary);
			}

			.nav-links a:hover,
			.nav-links a:focus-visible {
				color: var(--color-text);
			}

			.nav-submenu {
				position: absolute;
				top: calc(100% + 0.7rem);
				left: 0;
				min-width: 13rem;
				padding: var(--spacing-2);
				list-style: none;
				border: 1px solid var(--color-border);
				border-radius: calc(var(--radius) + 0.45rem);
				background: color-mix(in srgb, var(--color-surface) 96%, transparent);
				box-shadow: var(--shadow-dropdown);
				opacity: 0;
				visibility: hidden;
				transform: translateY(8px);
				transition:
					opacity var(--transition-fast),
					transform var(--transition-fast),
					visibility var(--transition-fast);
			}

			.nav-item--has-children:hover .nav-submenu,
			.nav-item--has-children:focus-within .nav-submenu {
				opacity: 1;
				visibility: visible;
				transform: translateY(0);
			}

			.nav-submenu li + li {
				margin-top: var(--spacing-1);
			}

			.nav-submenu a {
				width: 100%;
				padding: 0.7rem 0.8rem;
				border-radius: calc(var(--radius) - 0.25rem);
				font-size: var(--font-size-sm);
				font-weight: 600;
				letter-spacing: normal;
				text-transform: none;
			}

			.nav-admin {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				padding: 0.72rem 0.92rem;
				border: 1px solid color-mix(in srgb, var(--color-accent) 30%, var(--color-border));
				border-radius: 999px;
				background: color-mix(in srgb, var(--color-accent) 12%, var(--color-surface));
				font-family: var(--font-sans);
				font-size: var(--font-size-xs);
				font-weight: 800;
				text-transform: uppercase;
				letter-spacing: var(--tracking-wide);
				color: var(--color-text);
				transition:
					transform var(--transition-fast),
					border-color var(--transition-fast),
					background var(--transition-fast);
			}

			.nav-admin:hover {
				transform: translateY(-1px);
				border-color: var(--color-accent);
				background: color-mix(in srgb, var(--color-accent) 18%, var(--color-surface));
			}

			.site-search {
				position: relative;
				width: var(--search-input-width);
				--emdash-search-border-focus: var(--color-accent);
			}

			:global(.site-search-input) {
				width: 100%;
				min-height: 2.45rem;
				padding: 0.65rem 0.9rem;
				border: 1px solid var(--color-border);
				border-radius: 999px;
				background: color-mix(in srgb, var(--color-surface) 88%, transparent);
				box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.35);
				color: var(--color-text);
				font-family: var(--font-sans);
				font-size: var(--font-size-sm);
				transition:
					border-color var(--transition-fast),
					box-shadow var(--transition-fast),
					background var(--transition-fast);
			}

			:global(.site-search-input)::placeholder {
				color: var(--color-muted);
			}

			:global(.site-search-input):focus,
			:global(.site-search-input):focus-visible {
				outline: none;
				border-color: var(--color-accent) !important;
				background: color-mix(in srgb, var(--color-surface) 96%, transparent);
				box-shadow: 0 0 0 3px var(--color-accent-ring);
			}

			:global(.site-search-results) {
				position: absolute;
				top: calc(100% + var(--spacing-2));
				left: 0;
				right: 0;
				max-height: 26rem;
				overflow-y: auto;
				border: 1px solid var(--color-border);
				border-radius: calc(var(--radius) + 0.45rem);
				background: color-mix(in srgb, var(--color-surface) 96%, transparent);
				box-shadow: var(--shadow-dropdown);
				z-index: 20;
			}

			:global(.site-search-results .emdash-live-search-loading),
			:global(.site-search-results .emdash-live-search-no-results) {
				padding: var(--spacing-4);
				text-align: center;
				color: var(--color-muted);
				font-family: var(--font-sans);
				font-size: var(--font-size-sm);
			}

			:global(.site-search-result) {
				display: block;
				padding: 0.95rem 1rem;
				border-bottom: 1px solid var(--color-border-subtle);
				color: var(--color-text);
				font-family: var(--font-sans);
			}

			:global(.site-search-result:last-child) {
				border-bottom: none;
			}

			:global(.site-search-result:hover),
			:global(.site-search-result:focus),
			:global(.site-search-result.focused) {
				background: color-mix(in srgb, var(--color-accent) 8%, var(--color-surface));
				outline: none;
			}

			:global(.site-search-result .emdash-live-search-result-title) {
				display: block;
				font-weight: 700;
				font-size: var(--font-size-sm);
			}

			:global(.site-search-result .emdash-live-search-result-collection) {
				display: block;
				margin-top: 0.15rem;
				font-size: var(--font-size-xs);
				text-transform: uppercase;
				letter-spacing: var(--tracking-wide);
				color: var(--color-muted);
			}

			:global(.site-search-result .emdash-live-search-result-snippet) {
				display: block;
				margin-top: var(--spacing-1);
				font-size: var(--font-size-sm);
				line-height: var(--leading-normal);
				color: var(--color-muted);
			}

			:global(.site-search-result .emdash-live-search-result-snippet mark) {
				font-weight: 700;
				color: var(--color-text);
				background: transparent;
			}

			main {
				min-height: calc(100vh - var(--nav-height) - 25rem);
			}

			.site-footer {
				margin-top: var(--spacing-20);
				padding-bottom: var(--spacing-6);
			}

			.footer-inner {
				padding-top: clamp(2.8rem, 5vw, 4.5rem);
				padding-bottom: var(--spacing-8);
				border-top: 1px solid var(--color-border);
				border-radius: 2.5rem 2.5rem 0 0;
				background:
					radial-gradient(
						circle at top right,
						color-mix(in srgb, var(--color-accent) 12%, transparent),
						transparent 28%
					),
					color-mix(in srgb, var(--color-bg-subtle) 72%, white 28%);
				box-shadow: 0 -20px 60px rgba(17, 18, 30, 0.06);
			}

			.footer-grid {
				display: grid;
				grid-template-columns:
					minmax(0, 1.4fr)
					repeat(3, minmax(0, 0.9fr))
					minmax(0, 1.1fr);
				gap: var(--spacing-8);
				align-items: start;
			}

			.footer-brand,
			.footer-nav,
			.footer-widgets-section {
				font-family: var(--font-sans);
			}

			.footer-kicker,
			.footer-heading,
			.footer-widgets-section :global(.widget__title) {
				font-size: var(--font-size-xs);
				font-weight: 800;
				text-transform: uppercase;
				letter-spacing: var(--tracking-wider);
				color: var(--color-muted);
			}

			.footer-logo {
				display: inline-block;
				margin-top: var(--spacing-3);
				font-family: var(--font-display);
				font-size: clamp(2rem, 4vw, 2.8rem);
				line-height: 0.92;
				color: var(--color-text);
			}

			.footer-tagline {
				margin-top: var(--spacing-4);
				max-width: 24rem;
				font-size: var(--font-size-sm);
				line-height: var(--leading-relaxed);
				color: var(--color-text-secondary);
			}

			.footer-heading {
				margin-bottom: var(--spacing-4);
			}

			.footer-links {
				list-style: none;
			}

			.footer-links li + li {
				margin-top: 0.7rem;
			}

			.footer-links a {
				font-size: var(--font-size-sm);
				color: var(--color-text-secondary);
			}

			.footer-links a:hover {
				color: var(--color-text);
			}

			.footer-widgets-section :global(.widget-area) {
				display: block;
			}

			.footer-widgets-section :global(.widget) {
				font-family: var(--font-serif);
				color: var(--color-text-secondary);
			}

			.footer-widgets-section :global(.widget__title) {
				margin-bottom: var(--spacing-4);
				font-family: var(--font-sans);
			}

			.footer-widgets-section :global(.widget__content) {
				font-size: var(--font-size-sm);
				line-height: var(--leading-relaxed);
			}

			.footer-bottom {
				display: flex;
				align-items: center;
				justify-content: space-between;
				gap: var(--spacing-4);
				margin-top: var(--spacing-10);
				padding-top: var(--spacing-6);
				border-top: 1px solid var(--color-border);
				font-family: var(--font-sans);
			}

			.footer-copyright {
				font-size: var(--font-size-sm);
				line-height: 1.55;
				color: var(--color-muted);
			}

			.footer-copyright a {
				color: var(--color-text-secondary);
			}

			.footer-controls {
				display: flex;
				align-items: center;
				gap: var(--spacing-4);
			}

			.theme-switcher {
				display: flex;
				gap: 0.35rem;
				padding: 0.35rem;
				border: 1px solid var(--color-border);
				border-radius: 999px;
				background: color-mix(in srgb, var(--color-surface) 88%, transparent);
			}

			.theme-btn {
				display: flex;
				align-items: center;
				justify-content: center;
				width: 2.35rem;
				height: 2.35rem;
				border: none;
				border-radius: 999px;
				background: transparent;
				color: var(--color-muted);
				cursor: pointer;
				transition:
					transform var(--transition-fast),
					background var(--transition-fast),
					color var(--transition-fast);
			}

			.theme-btn:hover {
				transform: translateY(-1px);
				color: var(--color-text);
			}

			.theme-btn.active {
				background: color-mix(in srgb, white 60%, var(--color-bg));
				color: var(--color-text);
				box-shadow: var(--shadow-btn-active);
			}

			.theme-btn svg {
				width: 16px;
				height: 16px;
			}

			@media (max-width: 1180px) {
				.site-header__inner {
					grid-template-columns: auto minmax(0, 1fr);
				}

				.masthead__tools {
					justify-self: end;
				}

				.site-nav {
					grid-column: 1 / -1;
					order: 3;
				}

				.footer-grid {
					grid-template-columns: repeat(2, minmax(0, 1fr));
				}
			}

			@media (max-width: 860px) {
				.site-header__inner {
					grid-template-columns: minmax(0, 1fr) auto;
				}

				.site-nav {
					overflow-x: auto;
				}

				.nav-links {
					flex-wrap: nowrap;
					overflow-x: auto;
					padding-bottom: 0.25rem;
					width: 100%;
					justify-content: flex-start;
					scrollbar-width: none;
				}

				.nav-links::-webkit-scrollbar {
					display: none;
				}
			}

			@media (max-width: 640px) {
				.site-header__inner,
				.footer-inner {
					padding-left: var(--spacing-4);
					padding-right: var(--spacing-4);
				}

				.site-header {
					position: relative;
				}

				.site-header__inner {
					grid-template-columns: 1fr;
					gap: 0.75rem;
				}

				.masthead__brand {
					max-width: none;
				}

				.masthead__tools {
					justify-content: space-between;
					order: 2;
				}

				.site-nav {
					order: 3;
				}

				.site-search {
					width: min(100%, 11rem);
				}

				.nav-admin {
					width: auto;
					white-space: nowrap;
				}

				.footer-grid {
					grid-template-columns: 1fr;
				}

				.footer-bottom {
					flex-direction: column;
					align-items: flex-start;
				}

				.footer-controls {
					width: 100%;
					justify-content: space-between;
				}
			}
		</style>

		<script>
			document.addEventListener("keydown", (e) => {
				if ((e.metaKey || e.ctrlKey) && e.key === "k") {
					e.preventDefault();
					const searchInput = document.querySelector(
						".site-search-input"
					) as HTMLInputElement;
					if (searchInput) {
						searchInput.focus();
					}
				}
			});
		</script>
		<EmDashBodyEnd page={pageCtx} />
	</body>
</html>
